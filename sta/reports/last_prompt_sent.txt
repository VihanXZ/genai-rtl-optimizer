You are an expert RTL design engineer. You will be given a SystemVerilog module
with a timing violation of -5.94 ns (must become positive to pass).

## Original module
module tester1 (
    input  logic        clk,
    input  logic        rst,
    input  logic [31:0] data,
    input  logic [31:0] key,
    input  logic [2:0]  op,
    output logic [31:0] y
);

    logic [31:0] x1, x2, x3, x4;
    logic [31:0] checksum;
    logic [31:0] rotated;
    logic [31:0] transformed;
    logic [31:0] result;

    always_comb begin

        // Checksum calculation
        x1 = data + key;
        x2 = x1 ^ 32'hA5A5A5A5;
        x3 = x2 + {data[15:0], data[31:16]};
        x4 = x3 ^ {key[7:0], key[31:8]};

        checksum = x4 + (x3 ^ x1);

        // Data transformation
        transformed = (data ^ key) + x2;

        // Variable rotation
        rotated =
            (data << key[4:0]) |
            (data >> (32 - key[4:0]));

        case (op)
            3'b000: result = checksum;
            3'b001: result = transformed;
            3'b010: result = checksum ^ data;
            3'b011: result = data & key;
            3'b100: result = data | key;
            3'b101: result = rotated;
            3'b110: result = (checksum > key) ? 32'h1 : 32'h0;
            3'b111: result = checksum ^ transformed ^ rotated;
            default: result = 32'b0;
        endcase
    end

    always_ff @(posedge clk) begin
        if (rst)
            y <= 32'b0;
        else
            y <= result;
    end

endmodule

## Timing info
- Slack: -5.94 ns (NEGATIVE = VIOLATION)
- Clock period: 4.0 ns

## Gate-level analysis
Total path delay: 9.81 ns across 37 gates
Gate breakdown:
  - 14 x maj3 (MULTIPLIER/ADDER carry chain - VERY SLOW)
  - 5 x o21ai (compound gate)
  - 3 x nor2 (basic gate)
  - 3 x a21oi (compound gate)
  - 3 x a31oi (basic gate)
  - 1 x in (basic gate)
  - 1 x xnor2 (XOR logic)
  - 1 x a221o (basic gate)
  - 1 x o311ai (basic gate)
  - 1 x and3 (basic gate)
  - 1 x o22ai (basic gate)
  - 1 x xor2 (XOR logic)
  - 1 x o31a (basic gate)
  - 1 x dfxtp (basic gate)

Timing milestones along the path:
  Gate 0: 0.50 ns - in
  Gate 9: 3.26 ns - sky130_fd_sc_hd__a221o_1
  Gate 18: 5.30 ns - sky130_fd_sc_hd__a31oi_1
  Gate 27: 7.49 ns - sky130_fd_sc_hd__maj3_1
  Gate 36: 9.81 ns - sky130_fd_sc_hd__dfxtp_1

## STRICT RULES - READ CAREFULLY:
1. DO NOT simplify or restructure the math. DO NOT replace multiplications with
   shifts and adds. The synthesis tool (Yosys) already optimizes constant
   multiplies better than hand-written shift-add logic.

2. Your ONLY job is to split the combinational logic across pipeline stages
   by inserting flip-flop registers at strategic points in the computation chain.

3. To do this:
   a. Take the existing always_comb block
   b. Split the assignments into TWO groups
   c. Put the first group's results into registered (always_ff) pipeline registers
   d. The second group reads from those registers and computes the rest
   e. Keep each group's logic EXACTLY as-is (same operators, same constants)

4. Keep the original module name and port list exactly.
5. Output ONLY valid SystemVerilog. No markdown fences.
6. Add a brief comment about the pipeline latency.

Respond with the complete modified module only.
