You are an expert RTL design engineer specializing in timing closure through pipelining.

You will be given a SystemVerilog module and a timing report identifying its critical path,
including a gate-by-gate breakdown of where delay accumulates along that path. Propose ONE
pipelined version of the module that reduces the critical path delay by inserting registers
at the point where delay accumulates most, while preserving functional behavior.

## Original module
<<RTL_SOURCE>>

## Critical path summary
- Total path delay: <<PATH_DELAY_NS>> ns
- Slack: <<SLACK_NS>> ns
- Startpoint: <<STARTPOINT>>
- Endpoint: <<ENDPOINT>>
- Path depth: <<PATH_DEPTH>> gate stages

## Gate-by-gate delay breakdown
(cumulative arrival time, per-stage delay, and cell type at each point along the path --
net names are post-synthesis IDs, but the PATTERN of which cell types cluster and where
delay accumulates fastest is meaningful)
<<STAGE_BREAKDOWN>>


## Strict Constraints for Formal Equivalence (EQY)
To ensure the output passes EQY Sequential Equivalence Checking with a 1-cycle latency map:
1. DO NOT change, rename, or reorder ANY module ports.
2. PRESERVE the exact FSM state transitions and reset behavior (e.g. if the original uses an asynchronous reset like 
egedge reset_n, you MUST use exactly that).
3. PRESERVE memory addressing and array semantics.
4. DO NOT rewrite or restructure unrelated logic. Make the SMALLEST possible RTL diff.
5. The ONLY intentional architectural change must be adding pipeline registers to break the critical combinational cone (e.g., breaking an XOR dependency chain). You may choose the optimal number of pipeline stages, but you MUST report the exact total number of cycles of latency added across ALL output paths in the `LATENCY_DELTA_CYCLES` header so the equivalence wrapper can match it perfectly. Do NOT just register the final outputs.

## Instructions
1. The VERY FIRST TWO LINES of your response, before any comment block or code, must be
   exactly:
   // LATENCY_DELTA_CYCLES: <integer>
   // ESTIMATED_CRITICAL_PATH_NS: <float>
   where the first is how many EXTRA clock cycles of latency your design takes compared to
   the original (0 if unchanged), and the second is your best estimate of the new critical
   path delay in nanoseconds after your change (a positive number, always less than the
   original <<PATH_DELAY_NS>> ns). These two lines must be machine-parseable -- no extra
   words, punctuation, or explanation on either line.
2. Use the gate-by-gate breakdown to identify WHERE delay accumulates fastest -- that's
   usually the best place to insert a pipeline register, not just the midpoint by stage
   count.
3. If inserting a register adds a cycle of latency, also explain this in a comment near the
   top of the module.
4. Output ONLY valid SystemVerilog after the two required lines -- no markdown fences, no
   prose outside comments.
5. Preserve the original module name.
6. Add a comment block summarizing what changed, why, and which part of the gate-level
   breakdown drove the decision.

Respond with the two required lines, then the complete modified module.
